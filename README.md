# Waybar Configuration

## Обзор

Это улучшенная конфигурация Waybar для Sway Wayland compositor с фокусом на визуальную эстетику, функциональность и пользовательский опыт.

## Установленные модули

### Левая панель
- **Workspaces** (sway/workspaces) - переключение между рабочими столами
- **Recording Indicator** (custom/recording) - индикатор записи wf-recorder (показывается при активной записи)
- **Sway Mode** (sway/mode) - текущий режим Sway (например, режим изменения размера окон)
- **Media Player** (custom/media) - универсальный медиа-плеер через playerctl (поддерживает VLC, Firefox/Яндекс Музыка и др.)

### Центральная панель
- **Clock** (clock) - часы и дата с календарём (клик открывает gsimplecal)
- **Language** (sway/language) - индикатор раскладки клавиатуры (RU/US)

### Правая панель
- **System Tray** (tray) - системный трей
- **Privacy** (privacy) - индикаторы доступа к камере, микрофону и демонстрации экрана
- **Network** (network) - статус сетевого соединения (Wi-Fi/Ethernet)
- **Audio** (pulseaudio) - громкость и микрофон (скролл для регулировки)
- **Backlight** (backlight) - яркость экрана (скролл для регулировки)
- **Bluetooth** (bluetooth) - статус Bluetooth (клик открывает blueberry)
- **CPU** (cpu) - использование процессора
- **Memory** (memory) - использование оперативной памяти
- **Temperature** (temperature) - температура процессора
- **Keyboard State** (keyboard-state) - индикатор CapsLock и NumLock
- **Battery** (battery, battery#bat2) - зарядка батареи (две батареи при наличии)
- **Power Profile** (power-profiles-daemon) - профиль питания (performance/balanced/power-saver)

## Ключевые изменения и улучшения

### Визуальные улучшения

1. **Эффект стекла** - полупрозрачный фон с размытием (backdrop-filter: blur)
2. **Единый стиль** - все модули имеют одинаковую высоту, границы и отступы
3. **Индивидуальность** - каждый модуль имеет свой цвет и характер
4. **Улучшенные hover-эффекты** - плавные переходы и подсветка при наведении
5. **Улучшенные tooltips** - увеличены в 1.5 раза, с закруглениями и улучшенным стилем

### Русская раскладка
- **Очень заметное выделение** - красный фон с зелёной рамкой 3px
- **Пульсирующая анимация** - для привлечения внимания
- **Большой жирный шрифт** - для максимальной видимости

### Индикатор записи (wf-recorder)
- **Красная моргающая точка** с текстом "REC"
- **Показ длительности записи** в минутах и секундах
- **Клик останавливает запись** сразу без подтверждения
- **Размещён слева** после 5 вкладок

### Универсальный медиа-плеер
- **Использует playerctl** для поддержки множества приложений
- **Поддержка VLC и Firefox** (Яндекс Музыка)
- **Показывает последний активный источник** при нескольких играющих
- **Скрывается при остановке** воспроизведения
- **Клик для play/pause**, правый клик для следующего трека

### Pulseaudio улучшения
- **Динамическая ширина контейнера** - не перекрывает соседние элементы
- **Разделитель |** между громкостью и микрофоном
- **Микрофон всегда отображается**
- **Скролл для регулировки громкости**
- **Фиксированные отступы** для предотвращения наложения

### Scroll управление
- **Pulseaudio** - скролл вверх/вниз для громкости
- **Backlight** - скролл вверх/вниз для яркости

### Дополнительные модули
- **Network** - показывает Wi-Fi/Ethernet с названием сети и IP
- **Privacy** - индикаторы доступа к камере, микрофону и демонстрации экрана

## Удалённые модули

### MPD (Music Player Daemon)
**Статус:** ❌ Удалён
**Причина:** Не использовался пользователем
**Документация:** MPD - это локальный музыкальный сервер, требующий отдельной настройки. Если нужен, можно добавить обратно через модуль `mpd` в config.jsonc.

### Scratchpad
**Статус:** 💬 Закомментирован
**Причина:** Не использовался пользователем
**Документация:** Scratchpad - это функция Sway для временного хранения окон (как "корзина" или буфер). Окна можно скрыть в scratchpad с помощью `Mod+Shift+Minus` и восстановить с `Mod+Minus`.

## Скрипты

### recording-indicator.sh
Скрипт для индикации записи wf-recorder.

**Расположение:** `~/.config/waybar/scripts/recording-indicator.sh`

**Функциональность:**
- Проверяет, запущен ли wf-recorder
- Показывает "REC ●" при активной записи
- Отображает длительность записи в минутах и секундах
- Моргает для привлечения внимания
- При клике останавливает запись

**Зависимости:**
- `pgrep` - для проверки процесса
- `ffprobe` - для получения длительности видео

## Интеграция с Sway

Для полной функциональности индикатора записи убедитесь, что в `~/.config/sway/config` есть следующие привязки:

```
# Запись экрана с wf-recorder
bindsym --to-code $mod+Shift+r exec wf-recorder -g "$(slurp)" -f ~/Images/videos/record-$(date +'%Y-%m-%d-%H%M%S').mp4
```

При запуске записи через эту команду в waybar появится моргающий индикатор "REC ●".

## Конфигурация gsimplecal

Файл: `~/.config/gsimplecal/config`

**Настройки:**
- Увеличено окно календаря в 2 раза
- Увеличен размер шрифта до 18px
- Использована цветовая схема Catppuccin

## Работа с tooltips

**Ускорение показа:** Waybar показывает tooltips практически мгновенно при наведении курсора.

**Tooltips с дополнительной информацией:**
- **CPU** - использование и количество ядер
- **Memory** - подробная информация об использовании RAM
- **Network** - название сети, качество сигнала, IP-адрес
- **Battery** - оставшееся время и мощность
- **Temperature** - текущая температура и критическое значение
- **Clock** - полные секунды и календарь

## Цветовая схема

Основана на Catppuccin Mocha с адаптациями для каждого модуля:

- **Основной фон:** rgba(30, 30, 46, 0.85) - тёмно-синий с полупрозрачностью
- **Модули:** rgba(39, 39, 58, 0.9) - чуть светлее
- **Акценты:**
  - 🔵 CPU/Memory: светло-голубой
  - 🔴 Русская раскладка: красный с зелёной рамкой
  - 🟢 Запись: красный с морганием
  - 🟡 Bluetooth: серый/синий при подключении
  - 🔵 Privacy: оранжевый/зелёный/синий для разных типов
  - 🟢 Power: зелёный/жёлтый/голубой в зависимости от режима

## Запуск и перезагрузка

**Запуск:**
```bash
waybar
```

**Перезагрузка конфигурации:**
```bash
killall waybar && waybar &
```

Или используйте ваш менеджер сеансов или автозапуск.

## Зависимости

- `waybar` - основной бар
- `playerctl` - для медиа-плеера
- `wf-recorder` - для записи экрана
- `slurp` - для выбора области записи
- `ffprobe` - часть ffmpeg, для длительности видео
- `blueberry` - для управления Bluetooth
- `pavucontrol` - для управления звуком
- `brightnessctl` - для управления яркостью
- `powerprofilesctl` - для управления профилями питания
- `gsimplecal` - для календаря

## Требования к Sway

Убедитесь, что у вас установлены следующие пакеты для Waybar в Sway:
- `waybar`
- `sway`

Для полного функционала также необходимы пакеты из раздела "Зависимости".

## Кастомизация

### Изменение цвета модулей
Редактируйте `style.css` и ищите секции с `#module-name` для изменения цветов.

### Добавление новых модулей
Добавьте модуль в соответствующий массив в `config.jsonc` (`modules-left`, `modules-center`, `modules-right`).

### Изменение расположения модулей
Переместите модули между массивами в `config.jsonc`.

## Устранение проблем

### Индикатор записи не появляется
- Убедитесь, что wf-recorder запущен
- Проверьте, что скрипт `recording-indicator.sh` исполняемый: `chmod +x ~/.config/waybar/scripts/recording-indicator.sh`
- Проверьте путь к папке с видео в скрипте

### Медиа-плеер не показывает музыку
- Убедитесь, что playerctl установлен: `playerctl --version`
- Проверьте, что приложение играет музыку: `playerctl status`
- Протестируйте вручную: `playerctl metadata`

### Русская раскладка не выделяется
- Проверьте класс элемента языка в config.jsonc
- Убедитесь, что sway/language правильно настроен

## Дополнительные ресурсы

- [Waybar Documentation](https://man.archlinux.org/man/waybar.5.en)
- [Sway Documentation](https://man.archlinux.org/man/sway.5.en)
- [Playerctl](https://github.com/altdesktop/playerctl)

---

## Лицензия

Эта конфигурация может использоваться свободно.
